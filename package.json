{
  "dependencies": {
    "@types/node-fetch": "^2.5.10",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "start": "npm run build && echo Running... && node index.js",
    "build" : "echo Build Started && npx tsc index.ts && echo Build Done"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.1"
  }
}
